# ============================================================
# Migration Validator — Bronze Count Validation
# Generated  : 2026-08-12T14:21:17.933293
# Contains    : row count checks for all Bronze tables.
# Add tables  : re-run 'validate_cli.py multi' for more tables.
# ============================================================

tables:
  migration_test:
    validations:
      count_validation:
        source_table_name: migration_test
        source: postgres
        sourcequery: |
          SELECT COUNT(*) AS source_row_count
          FROM public.migration_test;
        target_table_name: migration_test
        target: snowflake
        targetquery: |
          SELECT COUNT(*) AS target_row_count
          FROM dev_edge_bronze.storedge_fms_public.MIGRATION_TEST
          WHERE _FIVETRAN_ACTIVE = TRUE;

  events:
    validations:
      count_validation:
        source_table_name: events
        source: postgres
        sourcequery: |
          SELECT COUNT(*) AS source_row_count
          FROM public.events;
        target_table_name: events
        target: snowflake
        targetquery: |
          SELECT COUNT(*) AS target_row_count
          FROM dev_edge_bronze.storedge_fms_public.EVENTS
          WHERE _FIVETRAN_ACTIVE = TRUE;

  general_ledger_line_items:
    validations:
      count_validation:
        source_table_name: general_ledger_line_items
        source: postgres
        sourcequery: |
          SELECT COUNT(*) AS source_row_count
          FROM public.general_ledger_line_items;
        target_table_name: general_ledger_line_items
        target: snowflake
        targetquery: |
          SELECT COUNT(*) AS target_row_count
          FROM dev_edge_bronze.storedge_fms_public.GENERAL_LEDGER_LINE_ITEMS
          WHERE _FIVETRAN_ACTIVE = TRUE;
    
  AcctSoftware:
    validations:
      count_validation:
        source_table_name: AcctSoftware
        source: postgres
        sourcequery: |
          SELECT COUNT(*) AS source_row_count
          FROM dbo.AcctSoftware;
        target_table_name: AcctSoftware
        target: snowflake
        targetquery: |
          SELECT COUNT(*) AS target_row_count
          FROM DEV_SITELINK_BRONZE.AWSDEV_DEVT5000_DBO.ACCTSOFTWARE
          WHERE _FIVETRAN_ACTIVE = TRUE;

  Addresses:
    validations:
      count_validation:
        source_table_name: Addresses
        source: postgres
        sourcequery: |
          SELECT COUNT(*) AS source_row_count
          FROM dbo.Addresses;
        target_table_name: Addresses
        target: snowflake
        targetquery: |
          SELECT COUNT(*) AS target_row_count
          FROM DEV_SITELINK_BRONZE.AWSDEV_DEVT5000_DBO.ADDRESSES
          WHERE _FIVETRAN_ACTIVE = TRUE;

  AcctSoftware:
    validations:
      count_validation:
        source_table_name: AcctSoftware
        source: postgres
        sourcequery: |
          SELECT COUNT(*) AS source_row_count
          FROM dbo.AcctSoftware;
        target_table_name: AcctSoftware
        target: snowflake
        targetquery: |
          SELECT COUNT(*) AS target_row_count
          FROM DEV_SITELINK_BRONZE.AWSDEV_DEVT5000_DBO.ACCTSOFTWARE
          WHERE _FIVETRAN_ACTIVE = TRUE;

  migration_test:
    validations:
      count_validation:
        source_table_name: migration_test
        source: postgres
        sourcequery: |
          SELECT COUNT(*) AS source_row_count
          FROM public.migration_test;
        target_table_name: migration_test
        target: snowflake
        targetquery: |
          SELECT COUNT(*) AS target_row_count
          FROM DEV_EDGE_BRONZE.STOREDGE_FMS_PUBLIC.MIGRATION_TEST
          WHERE _FIVETRAN_ACTIVE = TRUE;

